{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "functions": {
    "api/probe.js": { "maxDuration": 30 }
  },
  "rewrites": [
    { "source": "/api/probe", "destination": "/api/probe" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
